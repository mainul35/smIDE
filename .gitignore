target/
*.log
.idea/
*.iml
.smide/
app/
hs_err_pid*.log

# Written by the Java language server when it imports this project into a workspace.
# Per-machine, regenerated on demand, and ours: smIDE creates them by opening itself.
.classpath
.project
.settings/

# The crash report server's data when run from here: reports, and its token.
crash-data/
