id=com.smide.lombok
name=Lombok
version=0.1.0
vendor=smIDE
description=Lombok: runs it inside the Java language server, so the getters, setters and constructors it generates are known to the editor.
mainClass=com.smide.plugins.lombok.LombokPlugin
depends=
